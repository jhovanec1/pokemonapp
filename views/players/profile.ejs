<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Profile</title>
    <link rel="stylesheet" href="/css/app.css" />
</head>
<body>
    <h4>Welcome back, <%=players.name%>!</h4>
    <div id="playerprofile">
    <form action = "/players/profile/<%=players.id%>?_method=PUT" method="POST" >
        Username: <input type="text" name="name" value="<%=players.name %>" /><br/>
        Password: <input type="text" name="password" value="<%=players.password %>"/><br/>
        Email: <input type="text" name="email" value="<%=players.email %>" /><br/>
        Team:<select name="Team">
            <% for(let i =0;i<teams.length;i++){ let selected = ( i == 0
                ) ? "select" : ""; %>
                <option value = "<%=teams[i].id%>" select> <%=teams[i].name%></option>
            <% } %>
        </select> <br/>
        <input type="submit" name="" value ="Edit">
    </form>
    <form action = "/players/profile/<%=players.id%>?_method=DELETE" method="POST">
    <input type="submit" name="" value="Delete">
    </form>
    </div>
    <h5>Pokemon in your deck:</h5>
    <% for(let i = 0; i<players.Pokemons.length; i++){%>
        <li>
            <%=players.Pokemons[i].name%><br/>
            <img src="<%=players.Pokemons[i].img%>" width="50" height="50">
        </li>

    <% } %>
    
    <br/>
    <nav>
        <a href="/pokemon">Go Back to Pokemon</a>
    </nav>
</body>
</html>